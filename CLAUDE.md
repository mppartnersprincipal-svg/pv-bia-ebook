# Instruções do projeto — Claude

Use a memória abaixo como contexto inicial deste projeto. Ela é compartilhada com o Codex e contém o que já foi feito, as decisões aprovadas e as pendências.

@MEMORIA.md

Consulte os documentos detalhados apenas conforme a tarefa. Após alterações relevantes, atualize `MEMORIA.md` com o estado realmente verificado, mantendo o resumo curto e removendo informações superadas. As instruções atuais do usuário prevalecem sobre o histórico.

**Regra do usuário:** sempre ler `CLAUDE.md` e `MEMORIA.md` no início e, ao finalizar qualquer tarefa, atualizar ambos com um resumo enxuto do que foi feito, para que os próximos chats retomem sem gastar tokens relendo o projeto.
